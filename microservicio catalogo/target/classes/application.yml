server:
  port: 8085

spring:
  application:
    name: microservicio-catalogo

  datasource:
    url: jdbc:postgresql://aws-1-sa-east-1.pooler.supabase.com:5432/postgres?prepareThreshold=0
    username: postgres.bgkantafitezuiqrulhm
    password: duocplazanorte
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 1 # Limita a este microservicio a usar máximo 2 conexiones
      minimum-idle: 1      # Mantiene mínimo 1 conexión abierta

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

  instance:
    prefer-ip-address: true
    ip-address: 127.0.0.1
    hostname: 127.0.0.1
    instance-id: 127.0.0.1:${spring.application.name}:${server.port}
